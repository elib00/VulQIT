<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F0F0F0" /> <!-- Background color -->
    <corners android:radius="8dp" /> <!-- Rounded corners radius -->
    <padding
        android:bottom="4dp"
        android:left="4dp"
        android:right="4dp"
        android:top="4dp" />
    <stroke
        android:width="1dp"
        android:color="@color/cadmium_green" /> <!-- Border color (black) -->
</shape>
